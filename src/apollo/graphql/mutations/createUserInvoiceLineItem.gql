mutation createUserInvoiceLineItem($input: InvoiceLineItemInput) {
  createUserInvoiceLineItem(input: $input) {
    id
    description
    quantity
    unitPrice
    taxRate
    taxLabel
    currency
    amount
  }
}
