mutation askForNoumReference(
  $experienceId: ID!
  $reference: AskForReferencePayload
) {
  askForNoumReference(experienceId: $experienceId, reference: $reference) {
    experienceId
    _id
    providerName
    capacity
    referenceText
    imageUrl
    status
    __typename
  }
}
