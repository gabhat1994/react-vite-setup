import { SideNavigationDrawer } from './Drawer';
import { SideNavigationPersistent } from './Persistent';

export const NavigationContainer = {
  Drawer: SideNavigationDrawer,
  Persistent: SideNavigationPersistent,
};
