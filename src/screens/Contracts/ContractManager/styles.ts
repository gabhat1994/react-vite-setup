import styled from 'styled-components';
import { Card } from '@/components/Card';

const PageCard = styled(Card)`
  width: 100%;
  padding: 24px;
  overflow: visible;
`;

export default {
  PageCard,
};
