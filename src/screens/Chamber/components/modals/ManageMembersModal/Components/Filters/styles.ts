import { Stack } from '@/layout';
import styled from 'styled-components';

export const TabletFilterStack = styled(Stack)`
  border: 1px solid var(--border-card-neutral-default);
  border-radius: 8px;
`;
