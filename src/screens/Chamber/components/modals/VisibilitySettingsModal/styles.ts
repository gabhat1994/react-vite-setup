import { Separator as BaseSeparator } from '@/components/Separator/Separator';
import styled from 'styled-components';

export const Separator = styled(BaseSeparator).attrs(() => ({
  noMargin: true,
  fullWidth: true,
}))``;
