import styled from 'styled-components';

import { SearchUsersContainer as StyledSearchUsersContainer } from '@/features/events/styles';

export const SearchUsersContainer = styled(StyledSearchUsersContainer)`
  margin: 0;
  width: 100%;
`;
