import { Stack } from '@/layout';
import styled from 'styled-components';

export const PersonalEventContainer = styled(Stack)`
  padding: 8px;
  padding-left: 16px;
  border-radius: 8px;
  background: var(--bg-input-neutral-default);
  button {
    background: var(--bg-button-neutral-alt-default);
  }
`;
