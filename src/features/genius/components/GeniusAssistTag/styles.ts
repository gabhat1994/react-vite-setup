import styled from 'styled-components';

const Container = styled.button`
  background-color: var(--bg-tag-neutral-alt-default);
  border: 0;
  border-radius: 8px;
  cursor: pointer;
  padding: 2px 8px 2px 4px;
  width: auto !important;
`;

export default {
  Container,
};
