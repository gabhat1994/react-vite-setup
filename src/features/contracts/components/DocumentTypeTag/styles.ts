import styled from 'styled-components';
import { Tag } from '@/components/Tag';

const TypeTag = styled(Tag).attrs(() => ({
  size: 'small',
  contentFont: 'footnote-bold',
}))``;

export default {
  TypeTag,
};
